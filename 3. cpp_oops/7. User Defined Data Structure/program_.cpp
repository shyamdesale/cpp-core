/*    User-Defined Data Structure -

        Data structure created by the programmer, not built-in.
        Used to store related data in a structured/custom way.
        Defines how data is grouped, accessed, and manipulated.
        Implemented using class, struct, enum, union in C++.
        Helps model real-world entities (e.g., Cricketer, Student).
        Provides custom variables + behaviors together.
        Improves code organization, readability, reusability.
*/