#include<iostream>
using namespace std;
int main()
{
    float p = 1000;
    float r = 15;
    float t = 2;
    float si = (p * r * t)/100;
    cout<<"Simple Intrest is : "<<si;
    // simple intrest formula : (principle * rate * time) / 100
    return 0;
}