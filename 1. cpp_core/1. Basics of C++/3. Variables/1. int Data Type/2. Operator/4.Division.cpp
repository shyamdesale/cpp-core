#include<iostream>
using namespace std;
int main()
{
    int x;
    x = 5;
    int y;
    y = 3;
    cout<<x / y; //Division
    //Because both are integers, 5/3 does integer division, which means it drops the decimal part & keeps only the whole number result.
}