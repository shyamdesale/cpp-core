#include<iostream>
using namespace std;
int main()
{
    int x;
    x = 5;
    --x;
    cout<<x;
    // This program demonstrates the pre-decrement operator (--x) in C++. It decreases the value of x by 1 before using it,
    // so x changes from 5 to 4 and then is printed.
    return 0;
}