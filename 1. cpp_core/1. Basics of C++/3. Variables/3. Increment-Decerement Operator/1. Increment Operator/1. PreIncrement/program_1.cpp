#include<iostream>
using namespace std;
int main()
{
    int x;
    x = 5;
    ++x;
    cout<<x;
    //It increases the value of a variable before it is used in an expression.
    // x becomes 6 first, then x = 6
}