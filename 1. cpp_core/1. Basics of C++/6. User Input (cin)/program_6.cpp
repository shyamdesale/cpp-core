#include<iostream>
using namespace std;
int main()
{
    float base;
    cout<<"Enter a Base : ";
    cin>>base;
    float height;
    cout<<"Enter a Height : ";
    cin>>height;
    float area = (base * height) / 2;
    cout<<"Area of Traingle is : "<<area;
}